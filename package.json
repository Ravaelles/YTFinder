{
  "name": "ytfinder",
  "private": true,
  "scripts": {
    "install:all": "cd server && npm install && cd ../web && npm install",
    "build": "npm --prefix ./server run build && npm --prefix ./web run build",
    "test": "npm --prefix ./server run test && npm --prefix ./server run test:compile"
  }
}
